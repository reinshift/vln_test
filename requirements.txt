--index-url https://mirrors.aliyun.com/pypi/simple/
--trusted-host mirrors.aliyun.com

# Core ML dependencies for Qwen2.5-VL-7B model
torch>=2.4.0
torchvision>=0.16.0
torchaudio>=2.1.0

# Transformers with Qwen2.5-VL support (latest version)
transformers>=4.49.0

# Accelerate for efficient model loading and device mapping
accelerate>=0.25.0

# Image processing
Pillow>=9.0.0

opencv-contrib-python>=4.5
groundingdino-py

# Numerical computing
numpy>=1.21.0

# Additional ML utilities
safetensors>=0.4.0
tokenizers>=0.15.0

# JSON and regex (usually built-in but ensuring compatibility)
regex>=2022.0.0

# For model downloading and caching
huggingface-hub>=0.19.0

# Memory optimization
psutil>=5.8.0
